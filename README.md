#How to use this CLI app
## 1. Install requirements
$ pip install -r requirements.txt

## 2. Install setup.py
$ python setup.py develop

## 3. Run CLI app
$ autoML autoML --data-path 'data/Iris.csv' --target 'Species'

